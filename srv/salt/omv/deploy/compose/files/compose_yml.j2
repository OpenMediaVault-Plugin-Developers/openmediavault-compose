{{ pillar['headers']['auto_generated'] }}
{{ pillar['headers']['warning'] }}

# {{ file.name }}
# {{ file.description }}

{%- if datapath is not none %}
{{ file.body | replace("CHANGE_TO_COMPOSE_DATA_PATH", datapath) | replace("CHANGE_TO_COMPOSE_NAME", file.name) }}
{%- else %}
{{ file.body | replace("CHANGE_TO_COMPOSE_NAME", file.name) }}
{%- endif %}
